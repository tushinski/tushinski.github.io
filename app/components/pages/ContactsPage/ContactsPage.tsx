import React from "react";
import styles from "./ContactsPage.m.scss";

type Props = {
};

type State = {
};

export const ContactsPage: React.FC<Props> = (props) => {
    return (
        <div className={styles.home}>Contacts</div>
    )
}