export type EmploymentHistoryEntry = {
    position: string,
    employer: string,
    city: string,
    startDate: string,
    endDate: string,
    projectsDescription: string,
    responsibilities: string[],
    stack: string[],
}